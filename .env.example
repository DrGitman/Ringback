# Leave both unset to run against the local mock CALL-E transport (the default
# while onboarding is pending). Set CALLE_API_KEY once a real key is issued.
CALLE_API_KEY=
CALLE_BASE_URL=https://api.heycall-e.com
